<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Source-controlled release trust anchor. Every release package, verifier,
      installer, and upgrader reads this exact policy from the checked-out
      Launch Station source tree; a candidate app cannot supply a replacement Team ID
      or publisher on the command line.

      This policy is configured from the exact locally verified Developer ID
      Application certificate. Do not pass these values as installer arguments.
    -->
    <key>PolicyVersion</key>
    <integer>1</integer>
    <key>Configured</key>
    <true/>
    <key>TeamIdentifier</key>
    <string>6RWK4446NQ</string>
    <key>Publisher</key>
    <string>Jake Mawson</string>
</dict>
</plist>
